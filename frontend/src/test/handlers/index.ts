import { type RequestHandler } from 'msw';

// Feature handlers are added here as features are built.
// e.g. import { documentHandlers } from './document';
export const handlers: RequestHandler[] = [];
